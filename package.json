{
  "name": "speedy-van-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.9.0",
  "scripts": {
    "dev": "npm run dev --prefix apps/web",
    "build": "npm run build --prefix apps/web",
    "start": "npm run start --prefix apps/web",
    "lint": "npm run lint --prefix apps/web",
    "test": "npm run test:unit --prefix apps/web",
    "type-check": "tsc --noEmit --prefix apps/web",
    "prisma:generate": "npm run prisma:generate --prefix apps/web",
    "verify-logos": "node scripts/verify-logos.mjs",
    "svgo": "node scripts/svgo.mjs",
    "validate-catalog": "tsx scripts/validate-catalog-data.ts",
    "build-synonym-index": "tsx scripts/build-synonym-index.ts"
  },
  "devDependencies": {
    "tsx": "^4.20.4"
  }
}
